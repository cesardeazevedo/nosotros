import { css } from 'react-strict-dom'

const vars = {
  outlineColor: 'unset',
  outlineWidth: 'unset',
  outlineLegend: 'unset',
  labelPosition: 'unset',
}

export const textFieldTokens = css.defineVars(vars)
