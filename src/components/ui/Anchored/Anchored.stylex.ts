import { css } from 'react-strict-dom'

const vars = {
  offset: '0',
  translateX: '0',
  translateY: '0',
  originX: '0',
  originY: '0',
  scale: 1,
}

export const anchoredTokens = css.defineVars(vars)
