import { css } from 'react-strict-dom'

const vars = {
  containerColor: 'unset',

  iconColor: 'unset',

  toggleContainerColor: 'unset',
  toggleContainerColorSelected: 'unset',
  toggleIconColor: 'unset',
  toggleIconColorSelected: 'unset',
}

export const iconButtonTokens = css.defineVars(vars)
